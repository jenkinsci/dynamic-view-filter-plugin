<?jelly escape-by-default='true'?>
<j:jelly xmlns:j="jelly:core" xmlns:st="jelly:stapler">
  <j:if test="${it.delegate != null}">
    <j:set var="job" value="${it.getJobWrapper(job)}"/>
    <st:include it="${it.delegate}" page="column.jelly"/>
  </j:if>
</j:jelly>
